[tool:pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    retry: tests for retry mechanism
    idempotency: tests for idempotency key behavior
    integration: integration tests
